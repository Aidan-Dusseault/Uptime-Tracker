<h1>Edit <% @account.name %></h1>
  <%= form_for(@account) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
    <div class="forms">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <%= f.submit "Submit" %>
  <% end %>
